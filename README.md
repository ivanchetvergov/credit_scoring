# Credit Default Risk Prediction

Проект для предсказания риска дефолта по кредиту на основе данных Home Credit.

## 📋 Описание проекта

Этот проект реализует полный ML pipeline для задачи бинарной классификации:
- Feature engineering с агрегацией данных из нескольких источников
- Обучение baseline моделей (Logistic Regression, Random Forest, LightGBM)
- Кросс-валидация и оценка моделей
- Сравнение результатов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows

# Установить зависимости
make install
# или
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите исходные данные в директорию `data/raw/`:
- `application_train.csv`
- `bureau.csv`
- `bureau_balance.csv`
- `previous_application.csv`

### 3. Запуск pipeline

```bash
# Полный цикл: feature engineering + обучение всех моделей
make all

# Или по отдельности:
make features  # Создать feature store
make train     # Обучить модели
```

## 📁 Структура проекта

```
.
├── data/
│   ├── raw/              # Исходные данные
│   └── processed/        # Feature store (parquet)
├── src/
│   ├── config.py         # Конфигурация и константы
│   ├── features/
│   │   ├── feature_engineering.py  # Создание feature store
│   │   └── features.py             # Preprocessing pipeline
│   ├── models/
│   │   └── baseline_trainer.py     # Обучение моделей
│   └── reporting/
│       └── compare_models.py       # Сравнение результатов
├── scripts/
│   └── baseline_training.py        # Главный скрипт обучения
├── saved_models/          # Сохраненные модели
├── results/               # Результаты и метрики
├── requirements.txt
├── Makefile
└── README.md
```

## 🎯 Использование

### Обучение моделей

```bash
# Обучить все baseline модели
python scripts/baseline_training.py

# Обучить конкретные модели
python scripts/baseline_training.py logistic_regression random_forest

# Обучить без кросс-валидации
python scripts/baseline_training.py --no-cv

# Обучить без сравнения моделей
python scripts/baseline_training.py --no-compare
```

### Makefile команды

```bash
make help          # Показать все доступные команды
make install       # Установить зависимости
make features      # Создать feature store
make train         # Обучить все модели
make train-lr      # Обучить только Logistic Regression
make train-rf      # Обучить только Random Forest
make train-lgb     # Обучить только LightGBM
make train-no-cv   # Обучить без кросс-валидации
make clean         # Очистить временные файлы
make clean-all     # Полная очистка (данные + модели)
make all           # Полный pipeline
make quick         # Быстрый запуск без CV
make info          # Информация о проекте
```

## 🔧 Конфигурация

Основные параметры находятся в `src/config.py`:

```python
# Параметры обучения
SEED = 42
TEST_SIZE = 0.25
CV_FOLDS = 5

# Параметры моделей
MODEL_PARAMS = {
    'logistic_regression': {...},
    'random_forest': {...},
    'lightgbm': {...}
}
```

## 📊 Feature Engineering

Проект создает следующие типы признаков:

### Базовые признаки
- Числовые: доход, сумма кредита, возраст и т.д.
- Категориальные: тип контракта, образование и т.д.
- Бинарные: флаги документов, регион и т.д.

### Созданные признаки
- `CREDIT_INCOME_RATIO`: отношение кредита к доходу
- `ANNUITY_INCOME_RATIO`: отношение аннуитета к доходу
- `AGE_YEARS`: возраст в годах
- `EMPLOYMENT_YEARS`: стаж работы в годах
- `GOODS_PRICE_TO_CREDIT_RATIO`: отношение стоимости товара к кредиту
- `DAYS_EMPLOYED_ANOM`: индикатор аномальных значений стажа

### Агрегированные признаки
- **Bureau**: статистики по предыдущим кредитам
- **Bureau Balance**: статистики по просрочкам
- **Previous Application**: статистики по предыдущим заявкам

## 📈 Результаты

После обучения результаты сохраняются в:
- `saved_models/` - обученные модели (joblib)
- `results/` - метрики и сравнение моделей

Пример результатов:

```
MODEL COMPARISON TABLE
==========================================
                        Train ROC-AUC  Test ROC-AUC  Test Accuracy  ...
logistic_regression         0.7656       0.7623         0.6987     ...
random_forest               0.7806       0.7388         0.7117     ...
==========================================

🏆 Best model: logistic_regression
   Test ROC-AUC: 0.7623
```

## 🛠 Технологии

- **Python 3.8+**
- **scikit-learn** - машинное обучение
- **pandas** - обработка данных
- **numpy** - численные вычисления
- **LightGBM** - gradient boosting
- **joblib** - сериализация моделей

## ⚠️ Требования

- Python 3.8 или выше
- 8GB RAM минимум
- ~5GB свободного места на диске

## 📝 Логирование

Логи выводятся в консоль с уровнем INFO. Формат:

```
2024-11-06 10:30:00 - INFO - Starting Feature Store creation...
2024-11-06 10:30:05 - INFO - Feature store loaded. Shape: (307511, 245)
2024-11-06 10:30:10 - INFO - Training logistic_regression model...
```

## 🐛 Обработка ошибок

Все критические операции обернуты в try-except блоки с детальным логированием:

```python
try:
    df = pd.read_parquet(FEATURE_STORE_PATH)
except FileNotFoundError:
    logger.error(f"Feature store not found at {FEATURE_STORE_PATH}")
    logger.info("Please run feature_engineering.py first")
except Exception as e:
    logger.error(f"Error loading data: {e}", exc_info=True)
```

## 🤝 Contribution

При внесении изменений:
1. Сохраняйте стиль кода проекта
2. Добавляйте docstrings к функциям
3. Обновляйте конфигурацию в `config.py`
4. Тестируйте изменения перед коммитом

## 📄 Лицензия

MIT License

## 👤 Автор

Ivan

## 📞 Контакты

Для вопросов и предложений создавайте Issue в репозитории.